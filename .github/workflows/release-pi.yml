name: Raspberry Pi Image

on:
  # Dispatched by release.yml after creating the GitHub Release.
  # (release: published events from GITHUB_TOKEN don't trigger other workflows)
  workflow_dispatch:
    inputs:
      tag:
        description: Release tag (e.g. v0.5.2)
        required: true

permissions:
  contents: write

env:
  NODE_VERSION: "20"

jobs:
  pi-image:
    name: Build Pi image
    runs-on: ubuntu-latest
    timeout-minutes: 150
    env:
      VERSION: ${{ inputs.tag }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Build Programmer UI
        working-directory: openavc/web/programmer
        run: |
          npm ci
          npm run build

      - name: Build Simulator UI
        working-directory: openavc/web/simulator
        run: |
          npm ci
          npm run build

      - name: Strip leading v from version
        id: ver
        run: echo "version=${VERSION#v}" >> "$GITHUB_OUTPUT"

      - name: Set up QEMU for ARM emulation
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-user-static binfmt-support
          # pi-gen's build-docker.sh checks "which qemu-aarch64" (not qemu-aarch64-static)
          sudo ln -sf /usr/bin/qemu-aarch64-static /usr/bin/qemu-aarch64

      - name: Clone pi-gen (arm64 branch)
        run: git clone --depth 1 --branch arm64 https://github.com/RPi-Distro/pi-gen.git installer/pi-image/pi-gen

      - name: Package server archive
        run: |
          mkdir -p driver_repo plugin_repo projects/default
          mkdir -p installer/pi-image/stage-openavc/01-install-openavc/files
          tar czf installer/pi-image/stage-openavc/01-install-openavc/files/openavc.tar.gz \
            --exclude='__pycache__' \
            --exclude='*.pyc' \
            --exclude='.git' \
            --exclude='node_modules' \
            --exclude='.env' \
            --exclude='tests' \
            --exclude='.pytest_cache' \
            --exclude='.ruff_cache' \
            --exclude='installer/pi-image/pi-gen' \
            --exclude='openavc/web/programmer/node_modules' \
            --exclude='openavc/web/programmer/src' \
            --exclude='openavc/web/programmer/.env*' \
            --exclude='openavc/web/simulator/node_modules' \
            --exclude='openavc/web/simulator/src' \
            --exclude='openavc/web/simulator/.env*' \
            openavc/ \
            driver_repo/ \
            plugin_repo/ \
            installer/trusted-keys/ \
            requirements.txt \
            pyproject.toml \
            LICENSE

      - name: Stage update-helper.sh
        run: cp installer/update-helper.sh installer/pi-image/stage-openavc/01-install-openavc/files/update-helper.sh

      # The privileged action helper, from installer/ like everything else here.
      # It used to be written inline by the pi-gen stage script, which froze it
      # at image-build time; the release tarball carries the same file now so an
      # in-app update can refresh an already-flashed appliance.
      - name: Stage privileged helper
        run: cp installer/openavc-privileged-helper.sh installer/pi-image/stage-openavc/01-install-openavc/files/openavc-privileged-helper.sh

      - name: Stage firewall-sync.sh
        run: cp installer/firewall-sync.sh installer/pi-image/stage-openavc/01-install-openavc/files/firewall-sync.sh

      # Stage the systemd unit from installer/ (single source of truth). The Pi
      # image no longer ships a bespoke copy; this mirrors build.sh so the unit
      # the image installs matches the one the in-app updater syncs against.
      - name: Stage systemd unit
        run: cp installer/openavc.service installer/pi-image/stage-openavc/01-install-openavc/files/openavc.service

      # Stage the canonical starter project from installer/seed/default/ (the
      # single source of truth every deployment target ships). The image must
      # not carry its own copy — a committed fork can only drift.
      - name: Stage seed project
        run: cp installer/seed/default/project.avc installer/pi-image/stage-openavc/01-install-openavc/files/project.avc

      - name: Configure pi-gen
        env:
          SEMVER: ${{ steps.ver.outputs.version }}
        run: |
          cd installer/pi-image

          # Copy config and inject version
          cp config pi-gen/config
          sed -i "s/__VERSION__/${SEMVER}/g" pi-gen/config

          # Copy custom stage
          rm -rf pi-gen/stage-openavc
          cp -r stage-openavc pi-gen/stage-openavc

          # Fix permissions
          find pi-gen/stage-openavc -name "*.sh" -exec chmod +x {} \;
          chmod +x pi-gen/stage-openavc/prerun.sh

          # Skip stages 4 and 5
          touch pi-gen/stage4/SKIP pi-gen/stage4/SKIP_IMAGES
          touch pi-gen/stage5/SKIP pi-gen/stage5/SKIP_IMAGES

          # Only generate image after our custom stage
          touch pi-gen/stage0/SKIP_IMAGES
          touch pi-gen/stage1/SKIP_IMAGES
          touch pi-gen/stage2/SKIP_IMAGES
          touch pi-gen/stage3/SKIP_IMAGES

      - name: Build Pi image
        run: |
          cd installer/pi-image/pi-gen
          PRESERVE_CONTAINER=0 ./build-docker.sh

      - name: Collect image
        env:
          SEMVER: ${{ steps.ver.outputs.version }}
        run: |
          IMG=$(ls installer/pi-image/pi-gen/deploy/*.img.xz 2>/dev/null | tail -1)
          if [ -z "$IMG" ]; then
            echo "ERROR: No image found in deploy/"
            exit 1
          fi
          cp "$IMG" "openavc-${SEMVER}-pi.img.xz"

      - name: Upload Pi image to release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SEMVER: ${{ steps.ver.outputs.version }}
        run: |
          PI_IMAGE="openavc-${SEMVER}-pi.img.xz"

          # Publish the image with its own checksum sidecar. The Pi image is
          # built minutes-to-hours after release.yml has already published the
          # release, so this job must NOT mutate the shared SHA256SUMS.txt that
          # the in-app updater verifies against: a non-atomic delete-then-upload
          # (gh --clobber) could destroy every artifact's checksum on failure,
          # and a client checking for updates mid-rewrite would race a missing
          # file. A per-image sidecar keeps the shared checksums immutable after
          # publish; --clobber on the upload makes a re-triggered build
          # idempotent (the un-clobbered image upload used to fail on re-run).
          sha256sum "$PI_IMAGE" > "${PI_IMAGE}.sha256"
          gh release upload "$VERSION" "$PI_IMAGE" "${PI_IMAGE}.sha256" --clobber

      - name: Trigger marketing site rebuild
        run: |
          # --fail matters here. Plain `curl -s` exits 0 on a 404, so a hook URL
          # that was rotated, revoked, or pointed at a deleted site reports
          # success forever while the Pi image never appears on the download page.
          if [ -z "$SITE_DEPLOY_HOOK" ]; then
            echo "SITE_DEPLOY_HOOK is not set, so the Pi image never appears on the download page." >&2
            exit 1
          fi
          curl -sS --fail-with-body -X POST "$SITE_DEPLOY_HOOK" \
            -w '\nbuild hook responded %{http_code}\n'
        env:
          SITE_DEPLOY_HOOK: ${{ secrets.SITE_DEPLOY_HOOK }}
