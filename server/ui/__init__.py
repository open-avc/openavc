"""Facts about the panel's UI elements that more than one surface needs.

The panel renders elements; the Programmer IDE authors them; the cloud AI
writes them. When those three disagree about what a control needs, the panel
is the one that is right and the other two produce something broken. This
package is where the shared answers live so they can only be stated once.
"""
